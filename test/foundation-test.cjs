var tape = require("tape"),
    foundation = require("../build/foundation");

tape("foundation.render() has not been tested yet.", function (test) {
    test.equal(true, true);
    //test.equal(foundation.render(), "You're gonna have a bad time.");
    test.end();
});
